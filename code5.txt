思路记录：
第五题：
本题要求对传输的数据进行数据还原。首先，使用的是正常的数组进行遍历。然后，标记无效数据，记录有效数据条数，记录每条
有效数据的编号为一位数还是两位数，记录每条有效数据的字符长度。有效数据为0，直接输出并结束，若不为0，将有效数据条数
存入字符数组第一位，再处理每条有效数据4个字符的索引信息，并存入最后输出的字符数组。再然后，处理英文字母，按照数据
加密方式转换后存入最后输出的数组。最后，存入”.“为最后输出的字符数组的最后一位，然后输出这个字符数组。